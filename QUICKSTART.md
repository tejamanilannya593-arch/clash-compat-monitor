# 第一次使用

这是 Windows 上配合 Clash Verge Rev 的节点守护工具，不提供节点或订阅。当前为早期试用版：主开发环境是 Windows + Clash Verge Rev 2.4.5；其他版本和不同订阅还需要更多独立验证。

## 安装

1. 解压整个发布包，不要在压缩包内直接运行程序。
2. 启动 Clash Verge Rev，确认你的订阅本身可以连接。
3. 在 Clash 的 JavaScript 增强脚本功能中启用包内 `clash/enhancement.js` 并应用配置。增强脚本会增加统一节点组、独立探测组及本地 7896 探测端口，并调整相关服务分流。它支持节点直接写在 `proxies`、使用 `proxy-providers` 或两者混合的订阅，单节点订阅也可使用。已有增强脚本请先备份，不要直接覆盖。
4. 双击 `Diagnose.cmd`。`CONFIG_READY` 仅表示找到配置标记；其他状态会提示下一步。它不会修改配置。
5. 双击 `Install.cmd`，按提示完成。安装会检查内核连接、备份旧版、安装到当前用户目录并设置登录后自启动，不要求管理员权限。
6. 打开节点守护，选择长期使用的服务并保存。正常时可以关闭窗口，让它在托盘运行。

7. 如果要验证 ChatGPT 或 Gemini 的真实网页对话，按 [浏览器伴侣安装说明](browser-extension/README.md) 在 Chrome 或 Edge 中一次性“加载已解压的扩展”。保持账号登录，然后点击“自动实测当前节点”。浏览器伴侣会自动发送短消息、自动判断新回复，并自动关闭它创建的测试标签页；测试对话会保留，不自动删除。未登录时只需先登录一次，之后可重试。它不读取 Cookie、浏览历史、账号标识或既有对话；新回复只用于本次口令匹配，不保存正文。

v0.6.3-preview.5 会在当前节点验证通过后，让“🚀 节点选择”跟随“🌐 统一稳定节点”；程序自动切换时也会同步。它还逐站点比较 Google、GitHub 在探测入口和系统代理入口的连通结果，发现不一致时暂停自动切换。探测、直连、拦截组保持原样，不修改 Clash 配置文件。该功能只统一普通代理出口，不保证网站账号登录。可选网页入口检测仍不验证登录、搜索或下载。

缺少增强脚本时不会自动接管订阅。若 7896 已用于其他功能，请先解决端口冲突，当前版本不自动分配端口。

## 怎么判断运行正常

窗口会显示当前节点、最近检测时间和备用数量。“入口可达”不是“登录链路正常”，两者也不等于“账号实测通过”；备用为 0 时表示尚未找到符合条件的备用，并不必然意味着当前节点不可用。网页实测不等于 API 调用测试，也不保证之后每次模型生成都正常。账号实测记录在出口变化、满 30 天、规则升级或明确失败后自动失效，届时按提示再验证一次即可。

健康节点的性能寻优默认关闭；明确故障的连续失败容灾仍会工作。浏览器伴侣的后台自动实测也默认关闭：可以只实测这一次，或明确授权以后自动实测。失败后后台自动重试有 6 小时冷却，手动重试不受限制。未登录、验证码、安全挑战和暂不支持的网页结构会分别提示，不直接归因于节点故障；仅在消息已发出后出现明确错误或回复超时，程序才按保守规则复检旧节点。

若同一服务在当前节点和两个近期备用节点上出现同类异常，窗口会提示服务端异常观察。随后 10 分钟内程序仍检测其他服务，但不会把该服务故障记到节点名下，也不会因此切换；观察期结束后自动恢复检测。

“运行统计”显示本次启动的内存、CPU 时间、检测轮次及读取的响应正文量，可手动导出。数据不是网站可用率，也不是完整代理流量账单。重启会重新计数；需要比较一周体验时，请保留每天导出的文件。

## 退出、恢复和卸载

- 关闭窗口只会隐藏到托盘；使用托盘“退出”才停止程序。
- “暂停自动优化”会在当前请求结束后停止进一步检测和自动切换。
- “恢复上一个节点”仍会先复检。程序不保证已失效的旧节点能够恢复。
- 卸载运行 `scripts/uninstall.ps1`，会删除本程序、记录及自启动项；重要记录请先另存。Clash 增强脚本需在 Clash 中单独停用，不会随卸载自动删除。

## 报告问题

提供：操作系统版本、Clash 版本、复现步骤、预期与实际现象，以及可选的运行统计导出。`Diagnose.cmd` 的结果可以复制；不要上传 Clash 配置、订阅链接、完整日志或 `experience.json`，其中可能包含节点信息。项目不自动上传诊断或统计。

目前没有足够的多用户长期实测，不能宣称比 Clash 自带策略更快、更省流量或保证所有服务可用。
