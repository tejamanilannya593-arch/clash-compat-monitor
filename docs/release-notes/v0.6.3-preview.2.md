# v0.6.3-preview.2 — 检测路径一致性保护

本地预览版保留原有隔离探测入口。每轮先在当前节点上用 Google、GitHub 的普通连通请求对照独立探测入口与系统代理入口；任一入口均无法访问普通网站时，本轮暂停自动切换并在状态中说明。此检查不验证浏览器访问任何特定站点，也不改动 Clash 配置文件。

网页入口出现跳转或验证页时，详情显示“网站功能未验证”，避免将 HTTP 响应误读为网页功能可用。原有预览版与正式版不被本发布包覆盖，浏览器扩展协议仍为 0.6.2。
