# v0.7.0-preview.2 — 重启选择通知节点修复

- 即使带有完整 Trojan 等代理参数，也排除以“消息：”“公告：”“剩余流量：”等元信息标签开头的订阅条目。不会按香港、台湾或其他地区排除真实节点。
- 增强脚本清理通知条目的代理定义和组内引用；提供者使用兼容 Mihomo 的排除表达式。保留其他原有排除条件。
- 主“节点选择”组直接引用“统一稳定节点”，不再依赖两个独立的节点选择恰好一致；开启选择记忆。保留原有 Steam 下载直连规则。
- 软件跟随逻辑支持组引用，同时兼容尚未升级增强脚本的平铺选择组。
- 延续 0.7 运行时代理类型识别与不热重载整份配置的边界。
- 修复 Clash 全局 IPv6 设置覆盖增强脚本后，守护仅因 IPv6 开关开启而停止检测的问题；实际服务检测决定可用性，不修改全局网络设置。

升级需要同时应用包内增强脚本和软件，单独替换软件不能永久改变 Clash 组结构。运行故障容灾仍需要守护程序启动；不能保证账号登录、AI 对话或所有网站永久可用。这是本地预览版。
