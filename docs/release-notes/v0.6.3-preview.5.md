# v0.6.3-preview.5 — 逐站点识别代理路径不一致

探测入口和系统代理入口现在分别检查 Google、GitHub，再逐站点比较结果。即使两条路径各能连接至少一个网站，只要同一站点的结果不同，就会显示路径异常并暂停自动切换，避免把分流问题误判为节点问题。

保留 v0.6.3-preview.4 的普通代理组跟随能力；独立探测、直连、拦截组保持原样。本次没有修改 Clash 配置文件，也不保证特定网站账号登录。

这是本地预览包，不是 GitHub 正式版。
