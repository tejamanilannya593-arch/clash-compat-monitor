# v0.6.3-preview.3 — 保留共同故障时的容灾

修正上一个预览版的路径判断：只有独立探测入口与系统代理入口的普通网站连通结果不同，才认定为检测路径异常并暂停自动切换。两条路径均失败属于共同故障，不再被误判为路径不一致；原有的连续失败确认、候选节点复检和安全容灾流程继续生效。

其他检测与网页入口提示保持不变。本版本不修改 Clash 配置文件，也不对特定站点筛选节点。这是本地预览包，不是 GitHub 正式版。
