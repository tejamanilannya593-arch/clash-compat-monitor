# ClashCompatibilityMonitor v0.7.0-preview.14

## 本次修复

- 保存已选服务或性能寻优设置时，旧周期会在当前探测返回后的安全检查点退出，并用新设置启动下一轮。
- 旧周期的检测结果不会再触发候选切换、覆盖新界面状态，或产生虚假的检测故障提醒；正在进行的手动候选排行也不会发布旧服务列。
- 前十名网站延迟排行、完整复检、切换预算、观察和安全回退保持不变。

程序仍只操作现有代理组选择，不修改 Clash 配置或订阅文件。网站入口和登录链路检测不代表账号内真实对话通过。
