# v0.6.3-preview.4 — 普通代理组跟随稳定节点

“🚀 节点选择”现在会在“🌐 统一稳定节点”的当前节点验证通过后跟随同一个实际节点；程序自动切换或安全回退时也会同步。目标节点必须存在于普通代理组的可选项中，且重复检查不会重复写入。

“🧪 兼容性探测”继续独立运行；直连、拦截及其他服务组不被强制改写。程序仍只调用 Mihomo 的代理组选择接口，不修改 Clash 配置文件。同步出口不能证明具体网站可登录，账号、地区和网站本身仍需分别判断。

这是本地预览包，不是 GitHub 正式版。
